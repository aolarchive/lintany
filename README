= Lintany =

Lintany is a node script that enables monitoring and linting of a developers local project files. When files are changed in the developers local environments a number of configurable linters can be run on code based on it's file extension. Messages returned from the linters will be displayed in a growl notification (command line growl is required).

